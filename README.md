# layout-lab

Experimental browser laboratory for understanding layout engines, positioning, constraint resolution, and the path from declarative layout rules to rendered geometry.

The GitHub Pages site is intended to be the primary interactive surface: each experiment should explain one layout concept and make its inputs, computed geometry, and trade-offs visible.
